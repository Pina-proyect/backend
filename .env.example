DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pina_dev"

# OAuth - Google (añadir valores reales en entorno local/CI)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# Ejemplo de callback: http://localhost:3000/api/pina/auth/google/callback
GOOGLE_CALLBACK_URL=""

# JWT settings
# Secreto para firmar JWT en el backend
JWT_SECRET=""
# Tiempo de expiración del access token (ej: 15m, 1h)
JWT_EXPIRES_IN="15m"

# Tiempo de expiración del refresh token (ej: 7d)
JWT_REFRESH_EXPIRES_IN="7d"

# Bcrypt salt rounds (cost) para hashing de passwords (por defecto 10)
BCRYPT_SALT_ROUNDS="10"

# URL del frontend para redirección post OAuth
FRONTEND_URL="http://localhost:3001"